"""Service layer — importable business logic for portfolio, watchlist, and snapshots.

LLM auto-execution and HTTP handlers both call into these modules so that the
business rules live in exactly one place.
"""
